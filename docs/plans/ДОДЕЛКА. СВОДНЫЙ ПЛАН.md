# Доделка MTR-RAG-System — сводный план

**Статус:** ЗАПЛАНИРОВАНО (не начато)
**Дата:** 2026-09-06
**Основание:** Оценка реализации по docs/plans/*.md (подробности — в результатах аудита).
**Контекст:** LangGraph уже работает и покрыт тестами — оставляем, отклонение от плана фиксируем в DEVIATIONS.md. ML-классификатор (1A.4/1A.5) — упрощённый: rule-based + LLM-fallback, без обучения на 500+ сэмплах. LLM-работы — с живым OpenRouter (нужен ключ в .env).

---

## Фаза 0 — База (корреляция + LLM-доступ)

- [ ] 0.1 Фикс `request_id`: `backend/app/services/search_service.py:52` перезаписывает id вторым uuid. Убрать, использовать один id на весь пайплайн (audit-запись = executor-лог).
- [ ] 0.2 Фикс LLM-лимита: `backend/app/services/agent/llm/agent.py:32` `MAX_TOTAL_SECONDS=120.0` → 60.0 по плану; поправить сообщение «60 секунд».
- [ ] 0.3 Настроить живой OpenRouter (`llm/client.py`, `.env.example`); проверка здоровья LLM.
- [ ] 0.4 Создать `docs/plans/DEVIATIONS.md`: LangGraph вместо Planner/ExecutionPlan; репо-слой вместо провайдеров по именам; Qdrant и пр.

## Фаза 1 — Этап 1: парсинг и интенты

### 1A. Групповая классификация (сейчас MISSING)
- [ ] Модуль `parsing/classifier.py` → `GroupClassifier`: потребляет `group_keywords` + `contextual_overrides` из `DynamicRules` (данные загружаются `dynamic_rules.py:225-235`, но не read); rule-based скоринг 7 групп с confidence; контекстные переопределения («подбери+замену»→ЗАМЕНА, «расскажи/найди + паспорт/ГОСТ»→ДОКУМЕНТЫ, отрицание «нет», «сломался+замени»→РЕМОНТ); комбинированный (rule>0.7 берём; несколько равных → LLM-fallback; неуверенность → UNCLEAR).
- [ ] Поле `groups` в `ParsedQuery` (`app/schemas.py`); заполнять в `HybridParser`/`enrich_parsed`.
- [ ] Тесты `test_classifier.py`.

### 1B. Интенты
- [ ] Довести до 28: добавить `REPAIR_WITH_CHECKS` (детектор + требования); согласовать счёт (`intent/matrix.py:6`).
- [ ] Флаг `is_primary` для главного интента (`intent/detect.py:213-225`).
- [ ] Замапить `CHECK_SUFFICIENCY`, `ADD_COMPONENT` в `INTENT_TOOLS` (`tools/instruments.py`).

### 1E. Нормализация (почти отсутствует)
- [ ] `normalizers/`: `normalize_dn` (ряд R10), `normalize_pressure` (→МПа: бар/10, кгс/см2/10; сейчас cast в `parsers/pressure_parser.py:295`), `normalize_material` («Сталь 20»), `normalize_medium`/`normalize_climate` (через `DynamicRules.get_synonym`), `normalize_item_type`.
- [ ] Подключить в парсеры; возродить `normalizers/__init__.py`, `morph_normalizer.py` (закомментированы).

### 1F. LLM-доизвлечение параметров (fallback)
- [ ] Модуль `parsing/llm_extractor.py`: промпт с примерами для 28 интентов; валидация JSON/типов; приоритет у regex (не перезаписывать уже извлечённое); кеш (`llm/cache.py`, при Redis — TTL 24ч); метрики (вызовы/время/hit rate — база в `llm/client.py`).
- [ ] Вызов после rule-парсеров только при недобранных required-параметрах.
- [ ] Тесты offline с моком LLM (`test_llm_extractor.py`).

### 1H. Валидация в рантайм (функции не вызываются)
- [ ] В `enrich_parsed` (`intent/detect.py:342`) подключить `filter_params_for_intent` + `incompatible_detected`; конфликт интентов → UNCLEAR + маршрут уточнения (`clarify`).
- [ ] Тесты: несовместимая пара → UNCLEAR.

### 1K/1L. Словари и правила
- [ ] Мягкое удаление (`is_active=False`) вместо жёсткого в `admin.py`.
- [ ] Эндпоинт `POST /admin/validation-rules/reload`.
- [ ] Автопредложение правил/записей при импорте MTR (черновик для новых item_type).
- [ ] Тесты админ-эндпоинтов (сейчас только service-level).

## Фаза 2 — Этап 2: DAL и провайдеры

- [ ] Классы провайдеров по плану под репо-слоем (`services/agent/repository/`): `CatalogProvider` (get_component/search_by_params/get_all_*/история, маппинг COMP-→KSM), `StockProvider` (get_stock*/low/unused), `GraphProvider` (обернуть `neo4j_provider.py`: get_unit_structure/_edges, get_neighbors, is_installed_anywhere, get_uninstalled_components, get_all_units), дорасширить `PassportProvider`/`NormsProvider`.
- [ ] `DataAccessLayer` поверх провайдеров; контракт async (querying — уточнить: asyncpg vs run_in_executor).
- [ ] Кеш per-типными TTL (`redis_cache.py:21` сейчас 300с единый): компонент 1ч, типы/материалы 24ч, остатки 5м, граф 30м, соседи 15м, нормы 24ч, паспорта 1ч; ключи `catalog:*`, `stock:*`, `graph:*`, `norms:*`, `passport:*` с точечной инвалидацией.
- [ ] Qdrant по плану (сейчас одна коллекция `mtr_descriptions` = нормы, `config.py:26`): `norm_documents` для норм; `documents` для паспортов; `mtr_descriptions` — семантический индекс каталога (сейчас отсутствует).
- [ ] Import-пути: CSV/Excel каталог; паспорта (→очередь Celery); нормдоки (→`norm_documents`+`compatibility_rules`+Qdrant).
- [ ] JSONB schema migration (`attributes_schema_version`): скрипт-мигратор + проверка при старте (`main.py` lifespan).
- [ ] JSON-дублирование access-логов (план 2H.2).

## Фаза 3 — Паспорта и Celery (задачи-заглушки)

- [x] `workers/passport_worker.py`: `process_passport`/`reprocess_passport` — OCR (Docling/EasyOCR) → таблицы → параметры (rule+LLM) → `extracted_characteristics` → `suggest_ksm_links` → автопересвязка >0.8, `needs_review` 0.6–0.8.
- [x] `PassportProvider`: `extract_params`, `get_document_metadata`, `get_passport_text`, `link_passport_to_ksm`, `suggest_ksm_links`, `get_processing_status`; пороги из `validation_constants`.
- [x] Таблица `document_links` + модель + миграция 007.
- [x] Прогресс-эндпоинт: `/api/v1/passport/status/{id}` → состояние Celery-задачи (task_state/progress/stage из AsyncResult + прогресс в БД); + `/api/v1/passport/reprocess`. Загрузка сохраняет файл в `DOCUMENT_UPLOAD_DIR` и ставит задачу в очередь.
- [x] `celery_always_eager=True` для тестов + отдельная тестовая БД (план B.8.2). Тесты: `test_phase8_passport_pipeline.py` (загрузка→статус→извлечённые, автосвязь >0.8, review-зона 0.6–0.8, <0.6 без связки).

> Инфра-замечания к Фазе 3: Docling/EasyOCR вынесены в optional-dependency
> `pip install -e ".[ocr]"` (в образ-воркер). Без них задача завершается
> со статусом `error` с сообщением; upload/reprocess в API работают (файл
> сохраняется в `DOCUMENT_UPLOAD_DIR`, задача ставится в очередь). Статус
> `task_state/progress/stage` берётся из Celery `AsyncResult` только вне
> eager-режима (в тестах знаком — очередь не запущена).

## Фаза 4 — Авто-режим: C2 + метрики

- [ ] Реализовать `should_full_llm` (`verify/policy.py:20-30`, сейчас всегда False): severity=high + тип из `FULL_LLM_TYPES` → C2.
- [ ] В `_execute_auto` (`executor.py:149`) ветка полного LLM-перезапуска (`_execute_llm` с готовым parsed); логирование C1/C2.
- [ ] `get_status()` — блок `verify` (`executor.py:342`).
- [ ] Метрики `eval_40_auto.py`: распределение эскалаций (refine vs full_llm), токены, доля C2, сравнение deterministic/auto.
- [ ] Тесты: `test_policy.py` (реальное C2), `test_phase6_auto_mode.py` (новый маршрут).

## Фаза 5 — Качество ответов (Шаги 2–7 из `data/evaluation/PLAN_ИСПРАВЛЕНИЙ.md`)

- [ ] 5.1 (Шаг 2) `apply_stock_filters` (quantity_min/max/on_stock) + парсер `>N, <N, >=, <=`; кейсы AQ014/AQ015.
- [ ] 5.2 (Шаг 3) Ранние answer-ветки в роутерах; инструментов на справочный запрос 5→≤2.
- [ ] 5.3 (Шаг 4) `inventory_calculator` (множитель ×3, дефицит), `duplicate_detector` (группировка), `maintenance_planner` (текст+запчасти), `impact_analyzer` (соседи/несовместимые).
- [ ] 5.4 (Шаг 5) Офлайн-шаблоны ответов (`answer/builder.py`, `explanation.py`) по интентам; `mandatory_warning`→warnings.
- [ ] 5.5 (Шаг 7) Дефекты парсера: H2S↔CORR, жёсткий фильтр steel/medium для H2S, DN-переходы 219→159, ГОСТ-расшифровка.

## Фаза 6 — Хранилища Этапа 1.1 (незакрытое)

- [ ] Neo4j: узел `ComponentAlias` (COMP-SYN-XXX→KSM, constraint comp_id) + индексы `item_type`/`dn` (`scripts/GEN/generate_graph_neo4j.py:29-31`).
- [ ] Qdrant: коллекции/семантика из Фазы 2, миграция данных норм.

## Фаза 7 — Верификация

- [ ] Полный pytest (332+ теста, offline + stack-интеграционные).
- [ ] `python -m app.scripts.eval_40_auto` в режимах deterministic/auto/llm; фиксация метрик.
- [ ] Прогон кейсов `compose_check/*.txt` (8 сценариев), актуализация транскриптов.
- [ ] DoD по каждому этапу в конце фазы.

---

## Принципы

- Каждая фаза завершается зелёным тест-набором + прогоном eval.
- LLM-код — обязательно с graceful-fallback (паттерн `_execute_auto` уже есть).
- Отклонения от планов фиксировать в `docs/plans/DEVIATIONS.md`, не переделывать работающий код ради имён.