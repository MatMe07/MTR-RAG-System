# Доделка MTR-RAG-System — сводный план

**Статус:** АКТУАЛИЗИРОВАНО 2026-09-09 — большая часть пунктов уже реализована (пометки [x] проставлены по факту кода и тестов). Реальный остаток — в секции «Фактический остаток» внизу.
**Дата:** 2026-09-06
**Основание:** Оценка реализации по docs/plans/*.md (подробности — в результатах аудита).
**Контекст:** LangGraph уже работает и покрыт тестами — оставляем, отклонение от плана фиксируем в DEVIATIONS.md. ML-классификатор (1A.4/1A.5) — упрощённый: rule-based + LLM-fallback, без обучения на 500+ сэмплах. LLM-работы — с живым OpenRouter (нужен ключ в .env).

---

## Фаза 0 — База (корреляция + LLM-доступ)

- [x] 0.1 Фикс `request_id`: единый uuid на весь пайплайн (`search_service.py:28`, audit-запись = executor-лог).
- [x] 0.2 Фикс LLM-лимита: `MAX_TOTAL_SECONDS=60.0` (`llm/agent.py:32`), сообщение «60 секунд».
- [x] 0.3 Настроить живой OpenRouter (`llm/client.py`, `.env.example`); проверка здоровья LLM. *(ключ — runtime/env; в текущем окружении OpenRouter даёт 403 — см. DEVIATIONS.md)*
- [x] 0.4 Создать `docs/plans/DEVIATIONS.md`.

## Фаза 1 — Этап 1: парсинг и интенты

### 1A. Групповая классификация
- [x] Модуль `parsing/classifier.py` → `GroupClassifier` (rule-based скоринг групп + contextual overrides + LLM-fallback/UNCLEAR); подключён в `intent/detect.py:460`.
- [x] Поле `groups` в `ParsedQuery` (`app/schemas.py`); заполняется в `enrich_parsed`.
- [x] Тесты `test_classifier.py`.

### 1B. Интенты
- [x] 28 интентов: `REPAIR_WITH_CHECKS`, `CHECK_SUFFICIENCY`, `ADD_COMPONENT` в `intent/matrix.py` + маппинг в `INTENT_TOOLS` (`tools/instruments.py`).
- [x] Флаг `is_primary` для главного интента (`intent/detect.py` §1B.8, `schemas.py`).
- [x] Замапить `CHECK_SUFFICIENCY`, `ADD_COMPONENT` в `INTENT_TOOLS` (`tools/instruments.py:502-522`).

### 1E. Нормализация
- [x] `normalizers/`: `normalize_dn` (ряд R10), `normalize_decimal` (парсеры), `normalize_material`, `normalize_item_type` — подключены (`geometry_parser.py`, `parser.py`, `llm_extractor.py`).
- [ ] `morph_normalizer.py` так и не подключён (закомментирован в `hybrid_parser.py:18`,39) — минорный остаток, база работает через `normalizers.py`.

### 1F. LLM-доизвлечение параметров (fallback)
- [x] Модуль `parsing/llm_extractor.py` (промпт, валидация JSON/типов, приоритет regex, кеш); вызов в `intent/detect.py:419` при недобранных параметрах.
- [x] Тесты offline с моком LLM (`test_llm_extractor`).

### 1H. Валидация в рантайм
- [x] В `enrich_parsed` (`intent/detect.py:467,484`) подключены `filter_params_for_intent` + `incompatible_detected`; конфликт → UNCLEAR.

### 1K/1L. Словари и правила
- [x] Мягкое удаление (`is_active=False`) — поддерживается моделью/CRUD (`admin.py:166`).
- [x] Эндпоинт `POST /admin/validation-rules/reload` (`admin.py`).
- [x] Автопредложение черновиков правил при импорте каталога (1L) — `import_service._suggest_validation_rules` (`is_active=False`).
- [x] Тесты админ-эндпоинтов и импорта (`test_admin_reload.py`, `test_import_service.py`).

## Фаза 2 — Этап 2: DAL и провайдеры

- [x] Провайдеры под репо-слоем: `neo4j_provider`, `norms_provider`, `passport_provider`, `catalog_semantic_provider`, `documents_provider` (`repository/providers/`).
- [x] DAL-адаптер: `ToolDAL` (`tools/tool_dal.py`) поверх `IRepository` — фактически и есть DataAccessLayer (см. DEVIATIONS.md «Доступ к данным»).
- [x] Кеш per-типными TTL (`redis_cache.py` `DEFAULT_TTLS`: catalog 1ч, stock 5м, passport 1ч, словари 24ч).
- [x] Qdrant-коллекции разведены: `mtr_descriptions` (семантика/нормы), `norm_documents` (нормы), `documents` (паспорта) — значения заданы в `config.py:29-31`.
- [x] Import-пути: каталог/склад/граф через `import_service` + эндпоинты `/admin/imports/*`; паспорта — очередь Celery (`workers/`).
- [x] JSONB schema (`attributes_schema_version`) — модель/миграция 001.
- [x] JSON-дублирование access-логов (`repository/providers/access_logger.py`).

## Фаза 3 — Паспорта и Celery

- [x] `workers/passport_worker.py` (OCR → параметры → `suggest_ksm_links`, автосвязь >0.8, review 0.6–0.8), пороги из `validation_constants`.
- [x] `PassportProvider`-методы, таблица `document_links` + миграция.
- [x] Эндпоинты `/api/v1/passport/status/{id}`, `/reprocess`, upload → `DOCUMENT_UPLOAD_DIR` + Celery.
- [x] `celery_always_eager=True` для тестов; `test_phase8_passport_pipeline.py`.

## Фаза 4 — Авто-режим: C2 + метрики

- [x] `should_full_llm` (`verify/policy.py:20`) — high + тип из `FULL_LLM_TYPES` → C2.
- [x] `_auto_full_llm` (`executor.py:221`) + лог C1/C2; `get_status()` с блоком `verify`.
- [x] Метрики в `eval_40_auto.py` (эскалации refine/full_llm, токены, доля C2).
- [x] Тесты: `test_policy.py`, `test_phase6_auto_mode.py`.

## Фаза 5 — Качество ответов

- [x] 5.1 `apply_stock_filters` (quantity_min/max/on_stock) + парсер `>Н,<Н,>=,<=` (`parser.py:393-413`).
- [x] 5.2 Ранние answer-ветки в роутерах; «инструментов на справочный запрос ≤2» — реализовано (роутинг `REFERENCE` в `router.py` `is_reference_query` + `catalog_router`; `test_reference_routing.py`).
- [x] 5.3 `inventory_calculator`, `duplicate_detector`, `maintenance_planner`, `impact_analyzer`, `sufficiency_check` (`tools/analytic_tools.py`).
- [x] 5.4 Офлайн-шаблоны по интентам + `mandatory_warning`→warnings (5 кейсов AQ001/002/009/021/024 — закрыто 2026-09-09).
- [x] 5.5 Дефекты парсера: CORR/steel-middle H2S-фильтр, DN-переходы, ГОСТ-расшифровка (тесты `test_phase7_parser_fixes.py`).

## Фаза 6 — Хранилища Этапа 1.1

- [x] Neo4j: узел `ComponentAlias` + constraint `comp_id` (`generate_graph_neo4j.py`/`seed_stack.py`), индексы `Component.item_type`/`dn`.
- [x] Qdrant: миграция данных норм в `norm_documents` — 25 фрагментов засеяно (сетд в провайдере, `test_norms_via_qdrant`).

## Фаза 7 — Верификация

- [x] Полный pytest (410 passed; красный `test_document_manifest_uses_known_dcd_values` починён 2026-09-09 восстановлением `dcd_taxonomy.json`).
- [x] Полный pytest в контейнере зелёный — **418 passed, 12 skipped** (монтирование `./docs:/docs`, `mawo_pymorphy3`, изоляция Redis-снапшота/LLM-фабрики в тестах).
- [x] `eval_40_auto` deterministic: sources OK **40/40** (закрыт AQ013 — `duplicate_detector` получал `None`-репозиторий, источника `expert_decisions` не было).
- [x] Прогон кейсов `compose_check/*` (10 сценариев).
- [ ] DoD по каждому этапу — обновляется по факту (данный чек-лист).

## Фаза 8 — Качество LLM-промтов (закреплено, чтобы не потерять)

Аддитивные улучшения промтов (стиль не переписываем — только добавляем контракты):

- [x] Контракт ответа `action=finish` в `llm/agent.py::_INSTRUCTION`: прямой ответ (да/нет+число / перечисление), рекомендация, запрет выдумывать числа и факты, явное указание нехватки данных.
- [x] Справочник инструментов в `_build_initial_prompt`: «результат (поле result) — источник фактов, не додумывай; нет совпадений → так и напиши».
- [x] Примеры JSON-действий (`call_tool`/`ask_user`/`finish`) + правило эффективности (минимум вызовов, не повторять уже полученные данные, справочный запрос ≤1–2 тула).
- [x] `refine.py`: правило «используй ТОЛЬКО данные из структурированного ответа», в контекст добавлен `review_verdict`+`review_issues`.
- [x] `explanation.py`: в промпт добавлены `status` и `recommendations`, правило «не выдумывать коды/остатки».
- [x] Фикс `eval_40_auto`: режим `auto_llm` теперь честно идёт в auto-пайплайн с LLM-эскалацией (раньше молча падал в deterministic).
- [ ] Валидация промтов живым LLM: прогнать `eval_40_auto auto_llm` при доступной генерации OpenRouter + разбор транскриптов (проверить отсутствие «додумывания»). **Заблокировано окружением**: генерация 403 (см. DEVIATIONS); graceful-fallback проверен — refine корректно уходит в review (`llm_refine_failed=True`).

---

## Фактический остаток

- 1E: подключение `morph_normalizer` в `hybrid_parser.py` (минор, опционально; модуль работает, `mawo_pymorphy3`).
- Фаза 8: валидация промтов живым LLM + разбор транскриптов (см. критерий завершения).
- `docs/plans/Потом.md`: `purchase_order_preview` — выгрузка заявки на закупку (CSV/JSON) для 1С/ERP (вне текущей итерации).

## Принципы

- Каждая фаза завершается зелёным тест-набором + прогоном eval.
- LLM-код — обязательно с graceful-fallback (паттерн `_execute_auto` уже есть).
- Отклонения от планов фиксировать в `docs/plans/DEVIATIONS.md`, не переделывать работающий код ради имён.